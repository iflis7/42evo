#include <stdio.h>
#include <stdlib.h>

int main(int argc, char **argv)
{
    /*
                        --------- Behavoir -------
    --> argument (132) ==> returns :: one hundred and thirty-two
    --> argument (5987) ==> returns :: five thousand nine hundred and eighty-seven

                        --------- Cheks ----------
    ***** Your program can take up to 2 arguments
      1-If there is only one argument, it is the value you need to convert
      2-If there are two arguments, the first argument is the new reference dictionary
          and the second argument is the value you need to convert
      3- If the argument isn’t a valid unsigned integer, your program must return "Error",
           //followed by a newline
      4- Any memory allocated on the heap (with malloc(3)) must be freed correctly  
    */

   

   return (0);

}